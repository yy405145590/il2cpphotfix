// GENERATED BY UNITY. REMOVE THIS COMMENT TO PREVENT OVERWRITING WHEN EXPORTING AGAIN

buildscript {
    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.2.0'
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        flatDir {
            dirs 'libs'
        }
    }
}

apply plugin: 'com.android.application'


dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    //implementation 'me.weishu:epic:0.3.6'
    implementation 'com.android.support.constraint:constraint-layout:1.1.3'
    //implementation(name: 'andhook-lib-3.6.2', ext: 'aar')
}

android {
    compileSdkVersion 27
    buildToolsVersion '29.0.0-rc3'

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    externalNativeBuild {
        ndkBuild {
            path 'src/main/jni/Android.mk'
        }
    }

    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 27
        applicationId 'com.ly.update'
        ndk {
            abiFilters 'armeabi-v7a', 'arm64-v8a', 'x86'
            moduleName "il2cpp"
        }
        versionCode 1
        versionName '0.1'
    }

    lintOptions {
        abortOnError false
    }

    aaptOptions {
        noCompress = ['.unity3d', '.ress', '.resource', '.obb']
    }

    signingConfigs {
        release {
            storeFile file('F:/github/il2cpphotfix/sign/user.keystore')
            storePassword '123456'
            keyAlias '123456'
            keyPassword '123456'
        }
        debug {
            storeFile file('F:/github/il2cpphotfix/sign/user.keystore')
            storePassword '123456'
            keyAlias '123456'
            keyPassword '123456'
        }

    }

    buildTypes {
        debug {
            minifyEnabled false
            useProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt'
            jniDebuggable true
        }
        release {
            minifyEnabled false
            useProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt'
            signingConfig signingConfigs.release
        }
    }

    packagingOptions {
        doNotStrip '*/armeabi-v7a/*.so'
        doNotStrip '*/arm64-v8a/*.so'
        doNotStrip '*/x86/*.so'
    }

    bundle {
        language {
            enableSplit = false
        }
        density {
            enableSplit = false
        }
        abi {
            enableSplit = true
        }
    }
}
